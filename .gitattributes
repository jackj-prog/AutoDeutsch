# Normalize all text files to LF in the repo and working tree.
# (src/app.jsx historically had mixed CRLF/LF, which produced huge cosmetic diffs.)
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
