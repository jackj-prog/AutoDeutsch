# AutoDeutsch — Changelog

> Chronological build/work log. Append new entries at the TOP of the relevant
> section (newest first). Append-only by convention so concurrent agent edits
> merge cleanly. Migrated from the spreadsheet on 2026-06-20.

## Bugs & Fixes

| Build | Issue | Root cause | Fix | Status |
|---|---|---|---|---|
| 2026.06.20.22 | **Finishing a journey step dumped you on Home**, breaking the playthrough (do Learn → results → Home, instead of continuing the mission) | The results screen's actions were always `setScreen("home")` — no awareness that the session was launched from a mission step. | Results is now mission-aware: when `missionReturnRef` is set, the primary action is **"Continue mission →"** (`returnToMission`) which returns to the mission (step now ✓, next step ready) and **trims the dead session/results frames from history** so Back still goes to scenarios/Library. The dialogue "Listen" step already returns to the mission via the new `goBack`. Non-mission results unchanged. Verified Learn→results→Continue→mission→Back→scenarios. | Fixed |
| 2026.06.20.21 | **"Back" jumped to Home instead of the previous screen** when deep in a flow (e.g. Library → arc → scenarios → mission → Back dumped you on Home) | Every Back button hard-coded a destination (`setScreen("home"/"scenarios")`) rather than following navigation history — fine when there was one entry path, broken once Library/continue-hero also lead into the journey. | Added a real **back-stack**: an effect pushes the screen you leave on each forward change; bottom-nav tabs are roots that reset it; `goBack()` pops (Home fallback). Pointed every in-flow Back at `goBack` (session header, scenarios, mission, dialogues, Done, audio, browse). Verified Library→arc→scenarios→mission→Back→scenarios→Back→Library. | Fixed |
| 2026.06.20.20 | **Long German words clipped off both edges of the card** (e.g. "die Schlussfolgerung", "die Einsatzmöglichkeiten") | The recall/drill card rendered the word at a **fixed** font size (46/33/26px) with no wrapping inside an `overflow:hidden` card — anything past ~9 chars overflowed; long B2 compounds (16–20 chars) bled off-screen. **Missed by the visual audit, which only ever exercised short words.** | Added `fitWordSize(text)` — scales the size to the longest single token (compounds can't wrap mid-word), clamped to a readable floor — plus `overflow-wrap:anywhere` + `max-width:100%` as a safety net, applied to every big card-word (recall front/back, production prompt, article noun, plural). Regression guard: new `scripts/shoot.mjs longword` forces a long B2 compound to the card front. Verified "die Schlussfolgerung" fits on one line; zero overflow at 320px. | Fixed |
| 2026.06.20.14 | Trend/stats day-pruning could drop or keep the boundary day wrong in timezones behind UTC | `new Date("YYYY-MM-DD").getTime()` parses as **UTC** midnight, compared against a local `Date.now()` cutoff (app.jsx trend-prune ×2) — off by up to a day at the boundary. | Parse as local midnight: `new Date(k + "T00:00:00")` (matches the already-correct chart code). | Fixed |
| 2026.06.20.14 | Verb session length capped at 30 while there are 92 verbs | `cat === "__verb__" ? 30` hardcoded in both the mx and maxC ceilings, while every other mode derives its ceiling dynamically. | Use `VERBS.length` (92) in both places. | Fixed |
| 2026.06.20.14 | A session built from an empty pool (e.g. a category fully filtered out by the current level) navigated to a blank "0/0" dead-end | `startSession` didn't assert the built card list was non-empty before `setScreen`. | Added a `guardEmpty` check (vocab/article/plural branches) that bounces home with a "Nothing to practise — try another topic or level" notice instead of opening an empty session. | Fixed |
| 2026.06.20.14 | **WCAG-AA**: red `#DD0000` small text (~3.84:1) and disabled CEFR rows (`opacity:0.45` ~1.95:1) failed AA-normal; modals weren't focus-trapped | Brand flag-red used directly as small text; level rows dimmed with opacity; dialogs moved no focus and didn't trap Tab; Escape ignored the rank-up/onboarding overlays. | Added `RT` (#FF5A5A, ~6.9:1) for red *text* (deep R kept for rules/borders/badges); CEFR rows → `opacity:0.62`; modals (setup/settings/onboarding) now move focus in on open, trap Tab, and restore focus on close; Escape also closes the rank-up overlay + onboarding. (Per-route focus-on-nav deferred to avoid fighting session-input autofocus.) | Fixed |
| 2026.06.20.13 | Train-tab **Articles / Plural Forms** tiles didn't drill articles/plurals — tapping a preset ran a vocab session instead | The tiles open the setup with `setupCat="__all__"`, which `setupIsLibrary` treats as a *library* vocab session: it showed the Recall/Speak/Produce/Deep presets (which call `startSession` with **vocab** modes, discarding the `article`/`plural` mode) and hid the mode-respecting "Start session" behind "Advanced options". | Added `setupIsGrammarDrill` (`__all__` + mode article/plural). When set, the setup hides the vocab presets + library stats and shows the standard length config + a "Start session" that honours the drill mode; title/subtitle now read "Article Drill / der·die·das across every noun" (and Plural); `maxC` caps to the noun / plural-noun pool, not all 5,782 words. Verified end-to-end (tile → setup → "WHAT ARTICLE? ___ Matratze"). | Fixed |
| 2026.06.20.13 | Typed-drill "→" submit button bled ~4px past the right edge at 320px | The input + button flex row gave the `<input>` `flex:1` but no `min-width:0`, so it couldn't shrink below its intrinsic textfield width and pushed the button off-screen on the narrowest phones. | Added `min-width:0` to the shared `.ad-input` CSS rule — fixes every typed drill (cloze/plural/imperative/verb/dictation/weak/production) at once. Verified `SHOOT_OVERFLOW` reports 0 past-edge at 320px. | Fixed |
| 2026.06.20.10 | Journey (Scenarios) screen overflowed horizontally — mission can-do text + level chips pushed past the right edge | The mission-list `display:grid` had no `gridTemplateColumns`, so the implicit column was content-sized (min-width:auto); the `nowrap` can-do text never ellipsised and the row blew past the viewport. Same class as the earlier Library blow-out. | Grid → `minmax(0,1fr)` + `minWidth:0` on the card button, so the column caps at container width and the can-do text ellipsises. Verified zero overflow at 320 & 390px. | Fixed |
| 2026.06.19.10 | Beginners swamped with B2 vocab; level preference silently reset | After the deck grew to 5,782 words (37% B2), the default session level was the flat "all" pool, and the one-tap presets (Quick/Standard/Deep) hardcoded level:"all" and persisted it — overwriting the learner's level and pulling B2 words into A1 sessions. | setupLevel now defaults to "auto" (resolved to the learner's current CEFR level in filterPool, auto-advancing A1→A2→B1→B2); presets honour the level filter instead of forcing/persisting "all"; added an "Auto" selector option + hint; one-time migration of the legacy "all" default to "auto" (explicit bands kept). Fresh all-categories pool: 1,038 A1 cards, not 5,782 mixed. | Fixed |
| 2026.06.18.6 | Bottom tab bar raised up; dead gap (~1/8 screen) below it | The global overflow-x:hidden guard added in the fix above forced overflow-y:auto, turning the BODY into a scroll container; on mobile a fixed bottom bar then references the smaller layout viewport. | Removed the html/body overflow guard (overflow already prevented at source by the grid fixes). Body overflow-y back to 'visible'. Left a warning comment. | Fixed |
| 2026.06.18.3 | Horizontal scroll in Library → word Browse on narrow phones; the 'Known' button overhung the card edge | CSS grid blow-out: grid tracks are auto-sized and items default to min-width:auto, so cards couldn't shrink below their content. | Grid -> minmax(0,1fr); card minWidth:0; left column flex:1 + every text line ellipsised. Verified 280-390px. | Fixed |
| 2026.06.18.~ | Deploys not reaching devices ('nothing changed') | Service-worker CACHE_NAME wasn't bumped (a manual, easily-missed deploy step), so installed PWAs kept the cached bundle. | build now derives CACHE_NAME from a content hash of app.js+data.js+index.html — auto-invalidates every deploy. | Fixed |

## Session run-log

- **H4 (finish) — early build-sentences + correctness pass; HORIZON COMPLETE** (v.36, `src/data.js` + `scripts/h4-build-sentences.mjs`; `docs/NEXT-HORIZON.md`). Brought the early **A2/B1 missions (post, landlord, workhours, sim) from 1 → 3 build-sentences** each (SENTENCES 243 → 251) so a beginner's first missions have a full "Build the sentence" bonus — via an idempotent splice tool; the validator caught a **global-duplicate** sentence (`Die Heizung funktioniert nicht` already existed) and I swapped it for a unique one (`Der Aufzug ist kaputt`), a good catch for the correctness goal. Ran a **structural correctness check** over all 24 new dialogues (6–9 lines, de+en present, exactly 3 options, `correctIdx` in range — all clean). This completes **H4** and the **whole H1–H4 horizon** (v.29–36). Honest residue (non-blocking, content-lane): ~37 mid/late B1/B2 missions still have a single build-sentence (the bonus step works with one); a native-level German proofread of the new dialogues is a worthwhile future pass. 26/26 tests; SW cache bumped.

- **H4 (content depth) — every mission is now multi-scene** (v.35, `src/data.js` + `scripts/h4-second-scenes.mjs`; `docs/NEXT-HORIZON.md`). Closed the last open journey-content gap: the **24 single-scene missions each got a second dialogue**, so all **58 missions now have a 2-scene "Listen" step** (DIALOGUES 128 → **152**). Each new scene advances the scenario to a distinct beat rather than repeating scene 1 — e.g. steuerid → *Steuererklärung (getting money back)*, auslander → *extending the residence permit*, viewing → *the Bewerbungsmappe*, sick → *getting signed off at the doctor*, debate → *Homeoffice vs. Büro* — authored at each mission's level (B1/B2) with 7–9 lines + 2 comprehension questions, matching the existing schema. Added an **idempotent splice tool** (`scripts/h4-second-scenes.mjs`: inserts each dialogue and wires its title into the mission's `dialogues` array, skipping any already present) so the work is reproducible and reviewable. `npm run validate` OK (152 dialogues, every title resolves + wired), 26/26 tests, build + SW cache bumped. **Verified all 58 missions are multi-scene (0 single, 0 zero).**

- **H3 (step 2 / P3b) — daily-goal reframe: the daily loop as journey upkeep** (v.34, app.jsx; `docs/NEXT-HORIZON.md`). Completes H3. The home **"Today"** panel no longer reads as a raw card-count target: a new subtitle under the goal ring frames it as journey maintenance — **"Keep your journey sharp — {N} scenarios need a refresh"** when earned scenarios are slipping (from `journeyMaintenance`), else **"A few minutes keeps your earned scenarios sharp"** — and the goal-reached caption flips to **"✓ Journey kept sharp"** when nothing's decaying (else the usual "✓ Goal reached"). The card-count mechanic (ring, streak, 7-day bars, freezes) is untouched; only the framing now ties the everyday loop to the journey, matching the journey-aware reminder (v.33). Verified on the maintenance-state home; 320px clean. 26/26 tests; SW cache bumped. **H3 complete** (journey-aware reminders + daily-goal reframe; retention machinery — Notification-Triggers reminders + streak-freeze — pre-existed).

- **H3 (step 1) — retention: journey-aware re-engagement** (v.33, app.jsx; `docs/NEXT-HORIZON.md`). Started the return-loop horizon. Found the retention **machinery already in place and solid** — opt-in local reminders via **Notification Triggers** (`showTrigger`/`TimestampTrigger`: server-less scheduling of a week of notifications that fire even when the app is closed, with a foreground-timer fallback and reschedule-on-open) and a full **streak-freeze** system (banked freezes auto-absorb missed days). The real gap was that the nudge is **generic and disconnected from the journey**, so I made it **journey-aware**: a new `reminderBodyRef` is kept fresh from live state (`journeyMaintenance` + `currentMission`) and read at schedule time, so the reminder now reads **"{N} earned scenarios slipping — a few minutes keeps them sharp"** (the J5 maintenance nudge), else **"Continue your journey: {current mission}"**, else the generic line — re-engagement now pulls you back to the journey *by name*. Honest constraint recorded: **no backend → no true background push** beyond Notification Triggers (Chromium); iOS stays best-effort-while-open. Settings/reminder UI untouched (only the body computation changed); validate + 26/26. SW cache bumped. Remaining H3: P3b daily-goal reframe (home "Today" ring as journey upkeep).

- **H2 (step 2) — voice: two-way spoken roleplay** (v.32, app.jsx; `docs/NEXT-HORIZON.md`). Completed the voice loop: the roleplay now talks **back**. A persisted **"Voice"** toggle in the "Do it for real" header (default on, `rpVoice` + `localStorage ad-rp-voice`) auto-plays the other person's German lines via the existing `speak()` TTS — the `__BEGIN__` opener and every reply — so the café server / clerk is a real spoken back-and-forth, not just spoken input. Tapping the mic **cancels any playing TTS** (`speechSynthesis.cancel()`) so the reply isn't recorded into the next turn; TTS also stops when you leave the roleplay screen. With H2 step 1's mic input, a learner can now hold the whole scenario **out loud**: hear them, speak back, hear the reply. Verified via `roleplayvoice` (toggle + mic both render); 320px clean (busier header still fits). **Deliberately did not add voice to Production** — that's already the dedicated **Speaking** drill (English → say the German, ASR-graded); duplicating it would add nothing. Unifying the mic surfaces into one hook is deferred to the modularization lane (internal, not user-facing). 26/26 tests; SW cache bumped. **H2 essentially complete.**

- **H2 (step 1) — voice: speak the roleplay** (v.31, app.jsx; `docs/NEXT-HORIZON.md`). The flagship horizon win: the J4 "Do it for real" roleplay can now be **spoken**, not just typed — the leap from "type German" to "say German." Wired the existing Web `SpeechRecognition` (already used by Speaking-mode drills) into the roleplay chat with its **own recogniser** (`rpListening`/`rpRecogRef`, separate from the Speaking-mode one that auto-stops on the cards screen): a gold **mic button** in the input row, `de-DE`, `interimResults` streaming live into the input so the learner **reviews/edits before sending** (ASR isn't perfect); a red halo while listening (matches Speaking mode); the mic stops on send and whenever you leave the roleplay screen. **Graceful fallback:** where there's no `SpeechRecognition` (iOS Safari), the mic button is simply hidden and the placeholder reverts to type-only — exactly the existing no-ASR path. Verified via a new harness path `roleplayvoice` (stubs `window.SpeechRecognition` so the affordance renders headless); the verdict path is unaffected and 320px is clean. 26/26 tests; SW cache bumped. Next H2: spoken Production answers + one unified mic component.

- **H1 (step 2) — modularization: icon system → `src/components/icons.jsx`** (v.30, app.jsx + new `src/components/icons.jsx`; `docs/MODULARIZATION-PLAN.md` §0c). Continued the stateless Phase-1 extractions: moved the whole icon system (`ICONS` SVG path map, `SOLID_ICONS`, the `Icon`/`IconBadge`/`ProgressIcon` primitives, the `ICO_ANIM`/`ICO_KEYFRAMES`/`ICO_TIMING`/`ICO_EASE` tap-animation tables + `playIconTap`) — ~135 lines — out of `app.jsx` into `src/components/icons.jsx`. It reads `PAL` only at render/runtime (PAL is now in `src/lib/palette.js`, concatenated earlier), so it's safe; verified by `npm run validate` + 26/26 + **icons render across home/library/train/progress** (including the bottom-nav `ProgressIcon` bars). `app.jsx`: 6,655 → 6,520 lines; two module dirs (`src/lib`, `src/components`) now established. Moved with a Node slice script (135 lines is too many to hand-copy reliably). **Surfaced + recorded an ordering trap** (`MODULARIZATION-PLAN.md` §0c): files **sort alphabetically within `src/lib/`**, so a future `constants.js` holding `LEVEL_COLOR` (reads `PAL` at module-load) would concatenate *before* `palette.js` and TDZ-crash — the next `src/lib` file must carry only PAL-independent constants (or encode order in the filename). 26/26 tests; SW cache bumped.

- **H1 (step 1) — stabilize & sharpen: regression sweep + modularization kickoff + perf** (v.29, app.jsx + new `src/lib/palette.js`; `docs/NEXT-HORIZON.md`, `docs/MODULARIZATION-PLAN.md`). First session of the post-journey horizon (foundation work). **(1) Regression sweep:** ran the visual-audit harness across home/library/stats/browse/tutor/scenarios/mission/roleplay/journeypath/maintenance/progmax/arccomplete at 320px — **zero real horizontal overflow** anywhere (the only "past-edge" elements are the `aria-hidden` confetti pieces inside an `overflow:hidden` parent, no scroll introduced); spot-checked first-run + daily Home — **no regressions from the J1–J6 streak**. **(2) Modularization kicked off + proven:** extracted `const PAL` (the palette) out of the 6.65k-line `app.jsx` into **`src/lib/palette.js`** — the build already concatenates `src/lib`→`src/components`→`src/screens` before `app.jsx` into one IIFE scope, so it's the same shared binding (no import/export). Verified the real extraction by `npm run validate` + 26/26 tests + **identical render** (home/scenarios/stats screenshots — colours & icons intact). Recorded the proven pattern + the TDZ ordering rule in `MODULARIZATION-PLAN.md` §0b (corrected the "byte-identical" wording: that's about the inert plumbing; an extraction reorders source so verify by audit, not byte-diff). **(3) Perf assessed:** ~461 KB gzipped cold load, GH-Pages-gzipped + SW-cached after first load (one-time); vocab `V` (964 KB raw) dominates and is needed at first interaction, so no data-split warranted now (recorded as a low-priority future option). 26/26 tests; SW cache bumped. Next: continue Phase-1 stateless extractions (icon system → `src/components/icons.jsx`).

- **J6 — personalization + per-arc narrative framing** (v.28, app.jsx + `src/data.js`; `docs/JOURNEY-ROADMAP.md`). The final journey phase (app side): (1) **goal-based spotlight** — the Scenarios overview now reads "🎯 Your goal points at **{Arc}** — but the path builds up to it" and the matching chapter wears a **"🎯 YOUR GOAL"** chip, derived from the learner's stated intake goal (`Working in German` → The Job, `Settling in & making friends` → Belonging; the everyday/exam goals stay on the natural Touchdown start). Soft personalization — the sequential path is untouched, so beginners still build up rather than getting dropped into a B-level arc. (2) **Per-arc narrative intros** — a new `intro` field on all 7 `MISSION_ARCS` (distinct from the terse `sub` and the earned `payoff`) now opens each expanded chapter on the Scenarios path, so an arc reads as a story beat ("In Germany, life starts at the registration desk…"), not a folder — closing the small arc-intro follow-on noted in J3. Harness: `journeypath` now seeds a goal to exercise the spotlight/chip/intro. Verified + zero overflow at 320px. 26/26 tests; SW cache bumped. **This completes the J1–J6 journey build-out** (the remaining content-depth item — multi-scene coverage for the ~24 single-scene missions — is an additive content-lane job, see ROADMAP/HANDOFF).

- **J5 — unify progression + daily practice maintains the journey** (v.27, app.jsx; `docs/JOURNEY-ROADMAP.md`). Made the two parallel progress systems read as **one spine** and tied the habit loop back into the journey. **(A) Journey maintenance** — new `journeyMaintenance` (completed missions whose **scene words have gone due** in the SRS, via `MISSION_VOCAB` × `dueCards`). A green **"Keep your journey sharp — N earned scenarios slipping"** card now sits right under the Home journey hero, **naming the actual decaying scenarios** ("Order at a café · 12") and launching `startJourneyRefresh` — a targeted recall session of those exact earned-scenario words. Daily review is no longer generic; it's visibly *maintaining what you can already do*. **(B) One legible spine** — role is the **headline** (Newcomer→Local), **scenarios completed is the journey** (the headline metric), and **CEFR is the language-level sub-metric**. Progress panel 1 ("What you can do now") gains a **"Your journey · {done}/58 scenarios"** bar as the primary metric; panel 2 renamed **"Your journey to B2" → "Your language level · CEFR"** (kills the dual-"journey" collision); the Home CEFR card gets a **"Language level"** eyebrow to match. New harness path `maintenance` (post-load seed: 3 done missions with due scene words); Progress spine captured via `progmax`. Verified both + zero overflow at 320px. 26/26 tests; SW cache bumped. (Fifth of the J1–J6 build-out; **J6 (content depth/personalization) is the last** and largely a content-lane job.)

- **J4 — the mission roleplay climax ("Do it for real")** (v.26, app.jsx; `docs/JOURNEY-ROADMAP.md`). The biggest pedagogical upgrade: every mission now ends with a **live roleplay** where you *perform* the scenario instead of just drilling it. A gold **"The real thing · Do it for real"** climax card on the mission hub opens a focused chat in which the **context-aware Tutor stops tutoring and becomes the other party** (the café server, the clerk, the landlord…) — it greets you in German, stays in character turn-by-turn (`buildRoleplaySystem` + a `__BEGIN__` opener kick), and on **"Finish & see how I did"** drops character and returns a structured **verdict** (Passed / Almost / Keep practising + one "went well" + one "to improve", parsed from a `__GRADE__` turn). Passing/finishing credits a **bonus `roleplayed`** step (doesn't gate the 3-step completion). **Graceful no-key fallback:** without an Anthropic key it becomes a **scripted self-check** — the mission's real phrases (tagged SENTENCES, each with audio) to rehearse out loud + a "Could you do this for real?" self-rate that credits the step (with a nudge to add a key for the live version). Own chat state (`rpMsgs`/`rpVerdict`…), kept out of the Tutor history; its own roleplay system prompt; reuses the BYO-key Anthropic path. Harness: `roleplay` (no-key self-check) + `roleplaychat` (live chat + verdict, via a mocked Anthropic endpoint — request interception keyed on the message content, since the system prompt itself names the `__GRADE__` token). Verified all three states + zero overflow at 320px. 26/26 tests; SW cache bumped. (Fourth of the J1–J6 build-out; J1/J3/J2/J4 done, J5→J6 next.)

- **J2 — the journey as a felt path** (v.25, app.jsx; `docs/JOURNEY-ROADMAP.md`). Rebuilt the **Scenarios** screen from a flat list of arcs into a **map you walk**: (1) a top **overview header** — your role + "{done}/58 scenarios" + a progress bar + "Next: {role} at {n}" — so the whole trip reads at a glance; (2) each arc is now a **foldable chapter**, and by default only the **active** arc (the one holding your current mission) is open — completed and future chapters collapse to a one-line summary (icon, payoff/sub, "✓" or "{done}/{total}", chevron) so the screen focuses on **where you are**; the active arc wears a gold **"YOU ARE HERE"** chip; (3) inside an open arc the missions sit on a **connected vertical trail** — a spine line threads through node dots that fill **green** as you complete them (walked segments turn green too), your **current** step is a gold pulsing node reading "START HERE"/"CONTINUE →", and done can-dos mute. Added `arcOpen` fold state. New permanent harness path `journeypath` (`journeyMid` seed: Touchdown complete + Paperwork active) captures the green-trail / collapsed-complete-chapter / live "you are here" states; verified zero overflow at 320px. 26/26 tests; SW cache bumped. (Third of the J1–J6 build-out; J1/J3/J2 done, J4→J5→J6 next.)

- **J3 — Arc as a chapter** (v.24, app.jsx + `src/data.js`; `docs/JOURNEY-ROADMAP.md`). Gave the journey a felt **chapter rhythm**: completing the last mission of an arc now fires a **full-screen "CHAPTER COMPLETE" celebration** (green arc badge, "{Arc} complete", a one-line **payoff** — e.g. Touchdown → *"You've landed and found your feet."* — and a "Next chapter: {next arc} — {sub}" hint → "Start the next chapter →"), queued through `celebQueueRef` so it lands cleanly on Home behind any rank-up. Added a `payoff` line to all 7 `MISSION_ARCS`. New `arcCeleb` state + arc-complete detection in `markMissionStep` (fires only on the step that finishes the arc), flush + Escape wiring alongside the existing toast/rank-up overlays. The **Scenarios** arc header now reflects completion: green badge + the payoff line (instead of the sub) + "✓ Complete" when every mission in the arc is done. Permanent harness path `arccomplete` (`arcReady` seed: Touchdown 9-done + café 2/3, drive the café Listen step) — verified `screenshots/audit/j3-arccomplete.png`. 26/26 tests; SW cache bumped. (Second of the J1–J6 journey build-out; sequence J1→J3→J2→J4→J5→J6.)

- **J1 — Home becomes journey-first** (v.23, app.jsx; `docs/JOURNEY-ROADMAP.md`). Flipped Home's focal hierarchy so the journey is the flagship: the **gold "Continue your journey → {current mission}"** hero now leads Home (right under the greeting — role eyebrow + "All scenarios →", the can-do, and "{arc} · {level} · Next: {step}"); a brand-new user's first call to action is now the journey, not the word grind. The SRS **"Review N words"** Due action and the custom-session CTA are demoted to calm dark/outline cards, so there's a single gold focal point. `allDone` shows a "Journey complete — keep it sharp" hero. Verified on first + daily personas. 26/26 tests; SW cache bumped. (First of the J1–J6 journey build-out.)

- **Solo-operation handoff system + second dialogue scenes** (content, no app.jsx logic change). Set up `HANDOFF.md` — a living "resume here" single-pager (operating mode, current build, recently shipped, in-flight, open hand-offs, gotchas) so the returning agent isn't blind — and codified the recording discipline at the top of `AGENTS.md` (ship→CHANGELOG, state-change→HANDOFF, feature→spec, status→ROADMAP). Also **completed the content the other agent had in flight when it ran out of credits** (it was never committed): **6 new B1 second-dialogue scenes** giving single-scene relocation missions a richer "Listen" step (now 2 dialogues each) — `Was die Krankenkasse zahlt` (krankenkasse), `Zahnreinigung und Kontrolle` (dentist), `Rundfunkbeitrag — Befreiung` (rundfunk), `Die Kaution zurückbekommen` (kaution), `SCHUFA online bestellen` (schufa), `Lärm vom Nachbarn` (neighbours). Each title is wired into its mission's `dialogues` array (verified: the krankenkasse mission now reads "Listen to the scene · 2 dialogues"). DIALOGUES 122→128. `npm run validate` OK, 26/26 tests; SW cache bumped.

- **Syllabus→test loop wired** (v.19, app.jsx — consumes the content lane's two hand-offs). Closes the journey's learn→test gap end-to-end: (1) a mission's **"Learn the words"** + **"Say it out loud"** steps now drill that scenario's **exact words** (`MISSION_VOCAB[m.id]`, ~12) via a new `startSession` `opts.pool` path (keeps all SRS seeding/weighting; falls back to the broad category when no list / <5 words) — verified the café learn-step opens "Order at a café or bakery 1/12" with scene words (der Zucker…), and the sub-label now reads "12 key words". (2) Added a conditional **"Build the sentence"** bonus 4th step to the mission detail (only when the mission has tagged `SENTENCES`); it loads them into the existing sentence-builder ("Build · {cando}", e.g. "Was kostet ein Stück Kuchen?") and credits `built` on results — a **bonus** that doesn't gate the 3-step completion. So a mission now teaches its exact words → plays them in the scene → tests them in questions → produces them as sentences, one coherent word set. Per `docs/mission-vocab-spec.md` + `docs/journey-sentences-spec.md`. 26/26 tests; SW cache bumped.

- Content — **MISSION_VOCAB: learn exactly what you're tested on** (content lane, no app.jsx). Closes the syllabus→test loop: the mission "Learn the words" step today drills 12 random words from a broad category; now every one of the **58 missions has a curated word list — the exact scenario words drawn from its dialogue + questions** (576 refs, ~10/mission). Every word is a real `V` card (validated — a word not in the deck is a build error), so SRS still tracks it; learn → listen → questions → build-sentence now share one word set. Added `scripts/mission-vocab.mjs` (extracts the V words that actually appear in each scene) to generate/maintain candidates, and validator coverage. **Hand-off** (`docs/mission-vocab-spec.md` + `⬜ ready to build`): the app.jsx owner points `launchMissionStep`'s learn/speaking step at `MISSION_VOCAB[m.id]` (fallback to `cats`). `npm run validate` OK (58 mission-vocab lists, 0 missing words), 26/26 tests.

- Content — placement-test depth for the curated onboarding (content lane, no app.jsx): `PLACEMENT.items` 24 → 40 (now **10 per CEFR level** A1–B2). The placement is the first thing a new player does (P5), and the app shuffles a 3-per-level subset per attempt; 6/level made retakes repeat, 10/level keeps the onboarding fresh and the estimate more robust. +16 unambiguous graded items (sein-forms, Perfekt aux, weil/dass, Relativpronomen dessen, Konjunktiv II hätte, Frist/Bewerbung vocab, short-reading inferences). `npm run validate` OK (40 placement-items), 26/26 tests.

- Content — early-mission sentence depth for the curated player experience (content lane, no app.jsx): `SENTENCES` 219 → 243 (+24). The "Build the sentence" mission step was live-ready but the first-hours / most-trafficked missions had only one sentence each; brought every early mission (the 8 A1 Touchdown missions + cash/doctor/hotel/transit/gym) to **3 buildable sentences** — pay-by-card, "Kann ich die Rechnung haben?", "Wie geht es Ihnen?", "Können Sie das bitte wiederholen?", "Kann ich das anprobieren?", "Mir ist schlecht.", "Wann fährt der nächste Zug?" etc. So a new player's first missions feel rich, not sparse. 86 mission-tagged sentences total, all 58 missions covered. `npm run validate` OK, 26/26 tests.

- P6 — **Library → Curriculum; taxonomy → "Dictionary"** (v.18, per `docs/P6-curriculum-spec.md`). The Library tab led with the raw 36-category database and demoted the journey to one thin card — discovery by word-category, not life-situation. Flipped it: Library now **opens on the journey** — a gold continue-hero (`currentMission`: arc · level · can-do → `openMission`, "Continue/Start here") plus the full **7-arc map** with per-arc progress bars + `arcDone/arcTotal`, each row → the Scenarios screen. The 36-category taxonomy + the **Browse & search** button are demoted beneath an explicit **"Dictionary — every word by topic, a reference, not a path"** divider (taxonomy block unchanged, fully reachable). Reuses the existing Scenarios/Mission screens — no duplication, no data changes. Verified curriculum-first above the fold at 390px and zero overflow at 320px; 26/26 tests; SW cache bumped.

- Content — Amt/instruction imperatives (content lane, no app.jsx): `IMPERATIVES` 65 → 87 (+22). The drill had everyday verbs but none of the commands officials, forms and signs actually use; added **ausfüllen, unterschreiben, einreichen, abgeben, ankreuzen, buchstabieren, wiederholen, entschuldigen, folgen, umsteigen, aufschreiben, durchlesen, erklären, vorbeikommen, mitnehmen, einzahlen, überweisen, (sich) melden, vorzeigen, (sich) anstellen, drücken, (sich) wenden** — each with correct du/ihr/Sie forms + a relocation-flavoured example ("Füllen Sie bitte das Formular aus!", "Nehmen Sie Ihren Ausweis mit.", "Stellen Sie sich bitte hinten an."). `npm run validate` OK, 26/26 tests.

- Content — journey-linked sentence builders (content lane, no app.jsx): `SENTENCES` 157 → 219 by adding **62 mission-tagged sentence-builder items** (new optional `mission` field = a MISSIONS id) — **complete coverage: every one of the 58 missions across all 7 arcs now has ≥1 buildable sentence**. Each is a real utterance from that scenario (cash → "Ich möchte Geld abheben"; krankenkasse → "Ich möchte mich versichern"; givenotice → "Ich möchte die Wohnung kündigen"; interview → "Ich habe drei Jahre als Ingenieur gearbeitet"). Validator now enforces that a sentence's `mission` tag resolves. **Hand-off to the app.jsx owner** (`docs/journey-sentences-spec.md` + a `⬜ ready to build` claim): add a conditional "Build the sentence" 4th step to the mission detail screen that loads `SENTENCES.filter(s=>s.mission===m.id)` into the existing sentence-builder — so the journey gains a *produce-the-sentence* step on top of words/listen/speak. Additive + ignored until wired; general sentence drill unchanged. `npm run validate` OK (190 sentences, 33 tagged, 0 unresolved), 26/26 tests.

- Content — doubled the two weakest practice areas (content lane, no app.jsx): **`CONFUSIONS` 20 → 40** (+20 classic learner-error pairs: wann/wenn, ob/wenn, als/wie, schon/noch, viel/viele, wissen/können, dürfen/können, müssen/sollen, bevor/vorher, nachdem/danach, obwohl/trotzdem, weil/deshalb, für/seit, vor/seit, brauchen/benutzen, lassen/verlassen, Uhr/Stunde, Zeit/Mal, passen/passieren, antworten/beantworten) and **`EXAM` 12 → 24** (now 6 sets per CEFR level A1–B2, both Leseverstehen + Sprachbausteine: invitation, restaurant order, weather, bike ad, SMS, reservation, gym notice, complaint, account-switch forum, volunteering, noise complaint, lifelong learning). Directly deepens the now-live confusion drill + exam mode. `npm run validate` OK (40 confusion-pairs · 24 exam-sets), 26/26 tests.

- P7 increment 1 — **context-aware AI Tutor** (v.17, per `docs/P7-tutor-context-spec.md`). The Tutor's system prompt was static ("about B1 level — an electrical engineer relocating"). It's now built **per-chat from the learner's live data**: CEFR level (`deepStats.currentLevel`), relocation goal + country (`intakeAns`, now rehydrated from `ad-placement-v1` on load), status (`capability.role`), current scenario (`currentMission.cando`), and weak words (`weakPreview`). The starter chips are now data-driven too — role-play the current scenario ("…Order at a café or bakery. Spiel die andere Person.") and drill the learner's actual weak words ("Hilf mir, diese Wörter zu üben: …") — and the intro reads "pitched to your {level} level". Additive only: no change to the BYO-key flow or chat mechanics, no extra network calls. Full Tutor productisation stays deferred. 26/26 tests; SW cache bumped.

- Visual polish (v.16): **answer-reveal colour semantics.** On a wrong answer the *correct* answer was drawn in the same red (#DD0000) as an error — conflating "here's the right answer" with "you got it wrong". Now the correct answer always renders **green** (success = truth) in the production/vocab, article and plural reveals (matching the cloze drill, which already did this); the learner's wrong attempt stays red + struck-through ("You wrote …"). Clearer at a glance and more encouraging. Left the drill-card vertical rhythm as-is: the typed-mode top-anchor vs MC-mode centring is **intentional** (typed modes reserve the lower half for the on-screen keyboard, which balances the card on a real device). 26/26 tests; SW cache bumped.

- PWA / installability hardening (v.15): moved the **Montserrat `<link>` into `index.html` `<head>`** (+ `preconnect` to fonts.googleapis/​gstatic) instead of injecting it late from the React tree — the brand font now requests at first paint, no flash of fallback; the service worker already runtime-caches the CSS + woff2 (cache-first, CORS-enabled) for offline. **Completed `manifest.json`**: added `id` / `scope` / `lang` / `dir` / `categories` and split the icon `purpose` into proper `any` + dedicated `maskable` entries (was `"any maskable"` on both, which can crop on Android adaptive icons). 26/26 tests; SW cache bumped.

- A11y + correctness pass (v.14): see Bugs & Fixes. WCAG-AA red-text + CEFR-row contrast, modal focus-trap/focus-move + Escape for rank-up/onboarding, the UTC date off-by-one, verb-cap 30→92, and an empty-pool guard.

- Two visual-audit defects fixed (v.13): see Bugs & Fixes — Articles/Plural tiles drilling the right mode, and the 320px typed-drill submit-button overflow.

- Visual audit → **~100% coverage** (tooling+docs lane, no app/data change). Closed the last gaps with new harness paths: **tutor active chat** (seeds `gfc-ai-key` + a sample `gfc-tutor-msgs` conversation → live chat UI, zero network), **dictation**, **comprehension MCQ** (listening + questions), **revealed dialogue scene**, **mastery results recap** ("+1 Mastered ★" + 5/5 panel — the in-session burst is suppressed *by design*, app.jsx:2093), **confusion/exam answered**, **speaking reveal**, **library-category setup**, **onboarding intake**, and the full **milestone/progression family** from the prior pass (skill-unlock + role-up toasts, chapter checkpoint, full-screen rank-up, high-capability Progress) via a runtime vocab seed that reads the global `V`. **New finding:** typed-drill **"→" submit button overflows ~4px at 320px** (cloze/plural/imperative/verb/dictation/weak/production; fine at 390px) — input row not capped to viewport; logged in the audit doc. The only non-captured states are the deliberately-suppressed in-session mastery burst and live mic recognition. Every navigable screen + in-screen state now has a screenshot.

- Full visual-state audit (tooling+docs lane, no app/data change) → `docs/VISUAL-AUDIT-2026-06-20.md`. Capture-and-review of **every reachable screen/state** via `scripts/shoot.mjs` (puppeteer, real built bundle): ~52 of ~61 distinct states (~85%) — core nav (incl. advanced persona), journey + placement, every drill **session** (article/plural/cloze/verb/Konj-I/imperative/sentence/listening/confusion/exam/speaking/audio/weak), all feedback states (answered ✓/✗, capitalisation + ß nudges, drill grammar-note + reveal + green-bloom), and all results/celebrations (failed results, Perfect! 100% win, daily-goal / streak-milestone / freeze-used toasts). **Found 1 real defect:** the Train-tab **Articles/Plural tiles** open the `__all__` library setup, whose vocab presets (Quick/Speak/Standard/Deep) **discard the article/plural mode** — the mode-respecting Start session is buried under "Advanced options", so *Articles→Quick* runs a vocab session, not the drill (app.jsx owner). Confirmed the static audit's vertical-rhythm note (drill cards anchor inconsistently). Verified the "89% + ✓9 ✗0" results contradiction is a NON-issue (CountUp mid-frame; settles 100%). Harness fixes shipped: reveal the buried Start session for `__all__` drills (`traindrill`/`drillbloom`); `results` now drives via the home Production hero (loop 24→40); `resultswin`/`recall` start via the Quick preset + **focus the swipe card before keying** (ArrowRight=got-it, ArrowLeft=reveal) + 1.5s ring settle; new `weakspots` path. Remaining ~15%: mission-complete / role-up / skill-unlock celebrations + high-capability Progress (need mastery+mission seeding), in-session mastery burst, tutor chat (needs key), dictation — deferred to next pass.

- Parallel-work enablement — modularization prep (tooling/docs lane, no app/data change): to let each agent take a *big app-feature task* in parallel (not just one app epic + one content epic), the `src/app.jsx` single-writer bottleneck needs splitting into per-owner module files. Shipped the **build support for that, backward-compatible and verified**: `build-static.mjs` now concatenates `src/lib/` → `src/components/` → `src/screens/` before `src/app.jsx` into one compile unit (one IIFE scope, no ES imports needed). Verified the compiled `app.js` is **byte-identical** until those dirs exist, that a split file is compiled in, and that removing it restores identical output — so the monolith can be split incrementally, one shippable file at a time, with zero behaviour risk. Plus `docs/MODULARIZATION-PLAN.md` (target module map grounded in current line ranges + a 2-phase migration: stateless extractions first, then Context + screens) and a new AGENTS.md **"epic lanes"** protocol section (module-ownership map, contract-first specs, standing per-lane deploy). The split itself edits app.jsx, so the app.jsx owner runs it as a dedicated epic; everything else is ready.

- Confusion-pair drill + Exam mode wired (v2026.06.20.12): turned the content lane's last two un-surfaced datasets into live Train drills. **Confusion pairs** (Train tile → setup → drill): each `CONFUSIONS` item is a binary MC ("Which word fits?" — the two base words as options); on answer it reveals the filled sentence, the English, and the one-line `why`, plus the pair's rule. **Exam practice**: each `EXAM` question is an MC card with the reading passage shown above (scrollable) + a Show-English toggle (leseverstehen) and the `why` on reveal (sprachbausteine). Both flow through the existing drill MC pipeline (`handleDrillAnswer`), level-filter via `auto`, and feed the normal drill stats — no new engine. Verified confusion + exam render clean at 320px; 26/26 tests. The two lanes are now fully re-converged (all content-lane datasets surfaced).

- P5 placement flow (v2026.06.20.11): replaced the 3-field setup modal with a real first-run **journey intake → 2-minute placement test → result**. Intake asks country / reason / timeline / goal (the content lane's `PLACEMENT.intake`); the placement presents 12 MC items (3 per CEFR level, A1→B2, shuffled per attempt) from `PLACEMENT.items` with no per-question feedback; the result shows the estimated level (ladder rule: first level <50% correct), a personalized roadmap line, and the **first mission** for the learner's goal+level as the start CTA. Seeds the starting CEFR level (feeds the `auto` deck default — never a flat `all`) + persists `ad-placement-v1`; **no SRS writes**. Retake via **Settings → Placement test**. Built from the content lane's data + `docs/P5-placement-buildspec.md` hand-off — re-converges the two lanes. Verified intake/question/result render clean at 320px; 26/26 tests.

- Clean-sweep app audit (docs lane, no app/code change): full structure + visuals review → `docs/APP-AUDIT-2026-06-20.md`. Method: direct review of ~16 committed screenshots + code-level read of the shell/SW/manifest and the un-screenshotted Scenarios/mission render + 4 parallel deep-dives (architecture, data-wiring, content-quality, a11y/robustness/PWA). Snapshot taken at main 33e47fc — i.e. just **before** the P5/confusion/exam wiring above, so the audit's "unwired / ready to build" notes on those are now superseded (built in v.11/.12). **Headline: high-quality codebase** — SRS engine, persistence, and content are production-grade (content sweep: 0 correctness defects across full-corpus gender/conjugation/answer-key checks). **Two scary-looking screenshot artifacts are NON-issues** (verified against current code): the "2,001 words" counts are dynamic (`totalW=Object.values(V).flat().length`, live=5,782) and "−1.0/week" is gone (`perWeek=recent28/4 ≥0`). Real gaps still open for the **app.jsx owner**: a few WCAG-AA contrast/focus issues (0.45-opacity rows ~1.95:1; red `#DD0000` small text ~3.84:1; no focus-on-nav / modal trap), an unguarded empty-session-pool dead-end, a `new Date("YYYY-MM-DD")` UTC-vs-local off-by-one in trend pruning, Montserrat woff2 not precached, an incomplete manifest, and the verb-session cap hardcoded to 30 (VERBS now 92). Prioritized fix table in the doc §7.

- Content batch 7 — deepen the belonging arc (content lane, no app.jsx): 2 new B1 social-integration scenes — **Im Sportverein anmelden** (joining a club: trial session, monthly Beitrag, equipment, the Anmeldeformular) and **Sich für einen Integrationskurs anmelden** (the BAMF integration course: Berechtigung, ~700 hours, Eigenanteil, the final "Leben in Deutschland" test + B1 exam). New `belonging` missions `verein` + `integrationskurs`; Belonging arc 6 → 8 missions. Dialogues 120 → 122; missions 56 → 58. **Arc balance after this session's journey work:** Touchdown 10 · Paperwork 10 · Roof 7 · Money 8 · Health 8 · Job 7 · Belonging 8. `npm run validate` OK, 26/26 tests.

- Content batch 6 — deepen the roof arc (content lane, no app.jsx): 2 new B1 scenes for the most stressful real renting rites — **Die Wohnung kündigen** (giving notice: 3-month period, written/signed by post, deposit return after the handover, repainting only if the contract says so) and **Wohnungsbesichtigung mit vielen Bewerbern** (a competitive viewing: which documents to bring — Gehaltsnachweise, SCHUFA, Mietschuldenfreiheitsbescheinigung — ties to batch 5's SCHUFA scene). New `roof` missions `viewing` + `givenotice`; A Roof arc 5 → 7 missions. Dialogues 118 → 120; missions 54 → 56. `npm run validate` OK, 26/26 tests.

- Content batch 5 — more DACH bureaucratic rites (content lane, no app.jsx): 3 new B1 scenes for the named admin tasks relocators actually hit — **Den Führerschein umschreiben** (converting a foreign driving licence: translation, possible test, documents), **Ein Führungszeugnis beantragen** (certificate of good conduct for a new job: fee, processing, ID needed), **Eine SCHUFA-Auskunft holen** (credit report for a flat application: free annual copy, what Bonität means, the new-arrival empty-record problem). Wired into 3 new `paperwork` missions (`license`, `fuehrungszeugnis`, `schufa`), taking The Paperwork arc from 7 → 10 missions. Dialogues 115 → 118; missions 51 → 54. `npm run validate` OK, 26/26 tests.

- Content batch 4 — deepen the health arc (content lane, no app.jsx): 3 new B1 scenes that cover common, real DACH healthcare situations — **Beim Zahnarzt** (dentist: cavity, anaesthetic, what the Krankenkasse covers), **In der Notaufnahme** (A&E: fall, X-ray, Versichertenkarte, triage wait), **Termin beim Facharzt** (specialist referral / Überweisung + the real two-month waiting time). Wired into 3 new `health` missions (`dentist`, `specialist`, `emergency`), taking the Staying Well arc from 5 → 8 missions. Dialogues 112 → 115; missions 48 → 51. `npm run validate` OK, 26/26 tests.

- Content batch 3 — verb trainer + sentence builder (content lane, no app.jsx): **`VERBS` 60 → 92** — added 32 high-frequency verbs missing from the conjugation trainer: a block of strong/irregular verbs (tun, lassen, halten, fallen, gefallen, fangen, schlagen, waschen, wachsen, empfehlen, werfen, sterben, gewinnen, beginnen, schwimmen, singen, fliegen, verlieren, rufen, bitten, liegen, heißen), the mixed verbs kennen/nennen/rennen, and the **relocation-critical verbs** anmelden, umziehen, unterschreiben, überweisen, kündigen, ausfüllen, abholen — each with full present (6 persons), Perfekt (+ correct haben/sein), Präteritum, Konjunktiv II, Konjunktiv I and aux. **`SENTENCES` 147 → 157** — 10 relocation/Amt-themed sentence-builder items that drill word order with those verbs (separable-prefix split, modal + infinitive at end, weil/dass verb-final, Perfekt with sein/haben, Sie-imperative). Both feed existing drills (Verb Trainer, Sentence Builder) with zero app.jsx change. `npm run validate` OK (92 verbs · 157 sentences), 26/26 tests.

- Content batch 2 — deepen datasets + more scenes + harness + P5 hand-off (content/tooling/docs lane, no app.jsx): **(1) Deepened the practice datasets to usable depth** — `EXAM` 6 → **12 sets** (added the missing **A1 level** + a 3rd set at A2/B1/B2: opening-hours, postcard, SMS, colleague e-mail, WG-Zimmer ad, Bewerbungsanschreiben); `CONFUSIONS` 14 → **20 pairs** (+ leben/wohnen, bekommen/werden, lernen/studieren, **mieten/vermieten**, fragen/bitten, treffen/kennenlernen); `PLACEMENT` 16 → **24 items** (6 per level, so the test can draw a varied subset). **(2) More relocation scenes** for the thinner arcs — 4 new dialogues: *Eine Überweisung machen* (A2, → bank), *Die Nebenkostenabrechnung* (B1, new `nebenkosten` mission), *Nachbarn kennenlernen* (B1, new `neighbours` mission), *Telefonische Nachfrage zur Bewerbung* (B1, new `apply` mission). Dialogues 108 → 112; missions 45 → 48. **(3) Harness** — `scripts/shoot.mjs` gained ready-to-use `placement` / `confusion` / `exam` paths (kept out of `ALL`; pinned to the entry labels in the specs so the screens auto-shoot once built) + a usage note. Browser run not possible in this env (puppeteer dev-deps absent); instead verified at the data layer that every arc's earliest mission now opens with an A1 on-ramp and all new missions/datasets resolve. **(4) P5 hand-off** — `docs/P5-placement-buildspec.md`: a concrete app.jsx implementation spec (screens, ladder-scoring fn, state/storage keys, result→journey mapping, acceptance) so the app.jsx owner can build the placement flow without re-deriving it; specs reconciled to the new counts. `npm run validate` OK (112 dialogues · 48 missions · 20 confusion-pairs · 12 exam-sets · 24 placement-items), 26/26 tests.

- Content batch — A1 on-ramps + three practice datasets + P5 data (content/tooling/docs lane, no app.jsx): **(1) Extended the A1 survival tier beyond Touchdown** — 5 new A1 "einfach" entry scenes (Geld abheben, Mir geht es nicht gut, Einen Termin machen, Die Wohnung, Sich verabreden) wired as gentle on-ramps into the earliest mission of the money/health/paperwork/roof/belonging arcs, plus a new A1 `cash` mission (the money arc previously had *no* sub-B1 entry). Dialogues 103 → 108; missions 44 → 45; every arc now has an A1 way in. **(2) `CONFUSIONS`** — a 14-pair confusion dataset (kennen/wissen, legen/liegen, stellen/stehen, setzen/sitzen, seit/seid, das/dass, wenn/als, mögen/gefallen, bringen/holen, fahren/gehen, sagen/erzählen, denn/dann, wo/wohin, nach/zu), 4 gapped items each with a one-line `why` — feeds the planned confusion-pair drill (Top-10 Learning #9). **(3) `EXAM`** — 6 telc/Goethe-style sets (2× A2/B1/B2; Leseverstehen + Sprachbausteine formats) reusing the MC question shape, for the planned exam mode. **(4) `PLACEMENT`** — P5 narrative intake (country/reason/timeline/goal) + a 16-item graded placement bank (4 per CEFR level). **Tooling:** `validate-data.mjs` now validates all three new structures (ids, levels, ranges, sprachbausteine gap-count, per-level placement coverage) and reports their counts; negative-tested that each guard fires. **Docs:** `docs/P5-placement-spec.md` (intake, ladder scoring algorithm, result→journey mapping, app.jsx wiring) and `docs/practice-data-spec.md` (CONFUSIONS + EXAM shapes + intended drill/exam UX). All data is ready to wire when the app.jsx owner schedules the modes. `npm run validate` OK (45 missions · 14 confusion-pairs · 6 exam-sets · 16 placement-items), 26/26 tests.

- DACH-rite scenarios authored (content lane — `src/data.js`, no app.jsx): added 4 B1 dialogues — **Krankenversicherung anmelden**, **Der Rundfunkbeitrag** (GEZ), **Die Mietkaution**, **Steuer-ID und Lohnsteuer** — each 10 lines + 3 comprehension questions, natural relocation German. Wired into 4 new `MISSIONS`: `krankenkasse` (→ Staying Well, as the gateway before the doctor), `rundfunk` + `steuerid` (→ The Paperwork, after Anmeldung), `kaution` (→ A Roof, after the landlord). These give P4a's milestone/celebration engine the named DACH bureaucratic rites as real, levelled content. Dialogues 99 → 103; missions 40 → 44; validator resolves every reference. **Tooling:** `validate-data.mjs` now warns when a mission's declared CEFR level has no entry-level dialogue (catches the "upside-down journey" class P1 fixed). `npm run validate` OK, 26/26 tests. Content-only — no app.jsx edit; the content-hash cache-bump invalidates the deploy.

- P3a Home forward-momentum (v2026.06.20.09): the Home "Your mission" card now shows the concrete **next step** ("Next: Learn the words →") instead of an abstract step count, and the relocation **status** (Newcomer / Settling in / Resident / Local) shows as the section eyebrow — so the daily Home leads with *advancing the journey* and *who you're becoming*, not just the review queue. Deliberately low-risk: the streak / daily-goal / reviews engine is untouched (full goal-reframe is P3b). Reads currentMission + capability only.

- P4a capability rewards + identity (v2026.06.20.08): completing a mission now fires a **"Skill unlocked — You can now …"** celebration (the emotional payoff for a real-world can-do), and learners progress through a relocation **status** driven by skills earned (not card counts): **Newcomer → Settling in → Resident → Local**, celebrated on status-up. The current status shows as a badge in the Progress capability lead, with a "N more skills to become X" nudge. Reuses the existing celebration queue; reads missionProg only. (DACH-rite milestone *content* — Krankenversicherung, GEZ, Mietkaution, Steuer-ID scenarios — is queued for the content lane.)

- P2 can-do CEFR + capability Progress (v2026.06.20.07): the Progress screen now **leads with "What you can do now"** — a count of real-world can-dos earned (completed missions) + the next skill, tappable into the journey. The CEFR roadmap rows gained per-level **can-do counts** and a **telc exam-readiness chip** (ready / ~% / building, a vocab-strength estimate). The word-count, memory-strength, all-time and drill-coverage panels are demoted under a collapsible **"Under the hood — the numbers"** (default collapsed). Reads `missionProg`/`deepStats` only; no SRS/metric changes. Per docs/P2-capability-spec.md.

- Progress de-demoralising quick wins (v2026.06.20.06): the Stats journey panel led with the diluted whole-deck metric ("0.2% · 11 / 5,782 words") and a rank-up wall ("1,038 to rank up to A2"). Reframed to positive, reachable signals — "Across all levels · A1 → B2 · N words learned" (no tiny % / giant denominator) and "finish Chapter X of 5 — on your way to A2" (rank-up framed via the 5 reachable chapter checkpoints, not the raw word count). (The "−1.0/week" pace was a misread of the "~" approximation symbol — perWeek is gated ≥0, no bug.)

- P1 A1 survival tier (v2026.06.20.05): authored 12 A1 dialogues (greetings, introduce yourself, café/bakery, supermarket, paying, directions, restaurant, time, phone, weather, shopping) — dialogues A1 0→12 (total 99). Wired them into the 8 Touchdown missions, which now enter at **A1**, so a true beginner starts in real situations instead of wordlists. Fixes the upside-down journey.

- P0 Scenario/Mission spine shipped (v2026.06.20.04): promoted the 87 dialogues from the buried "Listening" drill into a first-class relocation **journey**. New `MISSIONS`/`MISSION_ARCS` manifest in data.js (40 missions across 7 arcs — Touchdown→Paperwork→Roof→Money→Health→Job→Belonging — covering all 87 dialogues; validator resolves every dialogue/category reference). New **Scenarios** screen (arc map + can-do mission cards with status + "START HERE"), **mission detail** ("After this you'll be able to…" + 3 steps: Learn words / Listen / Say it out loud, launching existing vocab/dialogue/speaking engines), and a **Home "Your mission" card** + a Library journey banner. Completion tracked in `ad-mission-progress-v1`, fully independent of the word-SRS (steps credit on session results / dialogue open). Added `plane`/`card` icons. No SRS/mastery/daily-loop changes — purely additive, per docs/P0-scenarios-spec.md.
- Learning Journey Audit (2026-06-20, planning only): assessed the whole experience through the lens "journey vs database." Root cause — progression is keyed entirely to vocabulary-strength counts, so CEFR/chapters/journey%/daily-goal all read as database coverage rather than real-world ability. Key deep-dive finding: the 87 dialogues are already a complete CEFR-laddered relocation curriculum (Arrival→Bureaucracy→Home→Money→Health→Work→Belonging) but are buried as the "Listening" tile in Train; dialogues are A1:0 / A2:32 / B1:23 / B2:32 (no A1 scenarios → journey is upside-down). Outcome: added the **Journey Transformation Plan** (P0–P7) to ROADMAP.md. No app code changed.
- Speaking production mode shipped (v2026.06.20.02): new EN→DE study mode where the learner speaks the German. Web-Speech `de-DE` recognition is matched against the target via `checkMatch` and feeds the same production-mastery track; where `SpeechRecognition` is unavailable (e.g. iOS Safari) it falls back to a self-graded shadowing flow. Added a `mic` icon, `speaking` to HERO_MODES, and surfaced it in the home Practice-mode row + topic setup.
- Level-aware sessions + "Auto" level (v2026.06.19.10 → .20.01): session level defaults to `auto` (resolves to the learner's current CEFR level), presets no longer clobber it, one-time migration of the legacy `all` default; home Level-focus control gained an Auto chip. Fixes beginners being swamped with B2 after the deck reached 5,782 words.
- Coordination migrated from the binary .xlsx to ROADMAP.md + CHANGELOG.md + AGENTS.md (2026-06-20).
- AI Tutor moved out of the immediate roadmap. It remains a strong future premium feature once AutoDeutsch launches publicly. Current priority should be progression, Home experience, session composition, guided learning, and production skills. Productizing the tutor makes more sense after the core product experience is polished and user demand is validated.

### Additional changes shipped this session (not in the Top-10 lists):
- Bug fix — horizontal scroll in Library / word Browse on narrow screens — Root cause was CSS grid min-width:auto blow-out (cards couldn't shrink, 'Known' button overhung). Fixed with grid minmax(0,1fr) + minWidth:0 + ellipsised text, plus a global html/body overflow-x:hidden guard. Verified clean 280-390px. (user-reported)
- Deploy reliability — service-worker cache auto-invalidation — build now derives CACHE_NAME from a content hash of app.js + data.js + index.html, so every deploy reaches installed PWAs (previously a manual, easily-missed step). APP_VERSION bumped each release for in-app verification (Settings -> Current build).
- Tooling — responsive overflow audit in the screenshot harness — Added SHOOT_WIDTH + SHOOT_OVERFLOW to scripts/shoot.mjs to catch this class of layout bug automatically at narrow widths.

### Deployment
All of the above deployed to main (GitHub Pages). Current build: 2026.06.18.3.

### Legend
Status: Done = implemented & deployed this session. In Progress = partially shipped. Not Started = unchanged.

### Official vocab add — progress
- Source: telc 'Einfach gut!' A1.1-B1.2. 2,055 new content words (function/multi-form filtered out), authored with example sentences, merged via scripts/merge_vocab.mjs into existing categories.
- Batch 1 (v2026.06.18.14): +73 — Greetings & Basics 20, Family & People 23, Clothing & Style 30. Vocab 2,001 -> 2,074.
- Batch 2 (v2026.06.18.15): +142 — Health & Doctor 42, Body & Health 60, Driving & Traffic 40. Vocab 2,074 -> 2,216. Running total added: 215 / ~2,055.
- Batch 3 (v2026.06.19.01): +101 — Admin & Bureaucracy 101. Relocation-critical Amt vocabulary (A2+B1). Running total added: 316/~2,055.
- Wave 1 (v2026.06.19.02): +662 across 27 categories — subagent-authored from telc A1-B1 lists (Food, Cooking, Shopping, Banking, Everyday Actions, Travel, etc.), each word re-categorised to the correct existing category with corrected glosses/articles/plurals. Vocab 2317->2979. Running total added: 978/~2,055.
- Wave 2 (v2026.06.19.03): +1018 across 35 categories — subagent-authored (Housing, Work & Study, Media, Engineering Workplace, Nature/Weather, Sport). Re-categorised + gloss/article/plural corrected. Vocab 2979->3997.
- Wave 3 (v2026.06.19.04): +16 final stragglers (school subjects, die Tasse, der Plan, der Blick, niemand/irgendwer/irgendwelche). Vocab 3997->4013.
- OFFICIAL VOCAB ADD COMPLETE: ~2,012 new telc A1-B1 words added this session (batches 1-3 + waves 1-3). Vocab 2001 -> 4013 across 36 categories. Function words, inflected forms, proper nouns and mis-glossed extraction artifacts were deliberately dropped. CEFR re-tag + official-list validation task closed out.
- QA sweep (post-add): heuristic check of all 2,012 new cards for cross-category duplicates, examples missing the headword, and suspicious glosses. 0 real defects — 91 flags were all false positives (separable verbs that split, umlaut plurals, and true cognates like fit/modern/beige). 0 cross-category duplicate words.
- CEFR coverage -> 100% (v2026.06.19.05): tagged the remaining 374 legacy off-telc-list cards from their curated diff field using the deck-wide empirical mode (easy->A1, medium->A2, hard->B1). Final levels A1 1038 / A2 1307 / B1 1249 / B2 419, 0 untagged. CEFR task fully closed.

### B2 add — Aspekte neu B2 (source: Jack; German-only PDF, 10 chapters, no English):
- Wave A (b0373cc, v2026.06.19.07): +591 — chapters 1-3,6 (Heimat, Sprache, Arbeit, Gesundheit).
- Wave B (v2026.06.19.07): +601 — chapters 4,5,8 (Zusammen leben, Wissenschaft, Geschichte).
- Wave C (v2026.06.19.08): +577 — chapters 7,9,10 (Kultur, Gefuehle, Zukunft). Repaired 3 cards truncated by an agent session-limit.
- ASPEKTE NEU B2 ADD COMPLETE (v2026.06.19.08): +1,785 B2 words via subagents (translate+categorise+author B2 examples from source collocations), consolidated to canonical categories, deduped, validated (26/26 tests). Deck 4,013 -> 5,782. B2-level content 419 -> 2,167. QA sweep clean. 100% CEFR coverage held: A1 1038 / A2 1307 / B1 1270 / B2 2167.
- Daily reminder shipped (v2026.06.19.09): opt-in Settings toggle + time picker; gentle local notification to defend the streak, skipped when the day’s goal is met. Uses the Notification Triggers API where available (fires when the app is closed; re-armed on open) with a foreground-timer fallback; SW notificationclick focuses/opens the app. No backend. Closes Next-Up #3 / Retention #5.
