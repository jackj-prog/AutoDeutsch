{
  "name": "autodeutsch",
  "version": "1.0.0",
  "private": true,
  "description": "Offline-first German learning PWA (static site, no runtime dependencies)",
  "type": "module",
  "scripts": {
    "build": "node build-static.mjs",
    "validate": "node scripts/validate-data.mjs",
    "test": "node --test tests/"
  },
  "devDependencies": {
    "@babel/standalone": "7.23.9"
  }
}
